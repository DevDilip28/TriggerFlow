export async function backpack(symbol: "USDT" | "ETH" | "SOL" | "USDC", tradeType: "buy" | "sell", qty: number, apiKey: string ){
    // console.log("Executing Backpack trade")
    // console.log(`Symbol: ${symbol}, Trade Type: ${tradeType}, Quantity: ${qty}, API_Key: ${apiKey}`);
};