export async function lighter(symbol: "USDT" | "ETH" | "SOL" | "USDC", tradeType: "buy" | "sell", qty: number, apiKey: string ){
    // console.log("Executing lighter trade")
    // console.log(`Symbol: ${symbol}, Trade Type: ${tradeType}, Quantity: ${qty}, API_Key: ${apiKey}`);
};