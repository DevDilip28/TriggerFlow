import { Router } from "express";

const workflowRoutes = Router();

workflowRoutes.post("/", );
workflowRoutes.put("/", );
workflowRoutes.get("/:workflowId", );
workflowRoutes.get("/execution/:workflowId", );
workflowRoutes.get("/node", );

export default workflowRoutes;
